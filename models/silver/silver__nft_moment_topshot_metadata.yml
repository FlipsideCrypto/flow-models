version: 2

models:
  - name: silver__nft_moment_topshot_metadata
    description: |-
      Cleaned metadata events for NBA TopShot from on-chain events.
    columns:
      - name: event_contract
        description: "{{ doc('event_contract') }}"
      - name: nft_id
        description: "{{ doc('nft_id') }}"
      - name: serial_number
        description: "{{ doc('serial_number') }}"
      - name: play_id
        description: "{{ doc('play_id') }}"
      - name: set_id
        description: "{{ doc('set_id') }}"
      - name: set_name
        description: "{{ doc('set_name') }}"
      - name: subedition_id
        description: "{{ doc('edition_id') }}"
      - name: metadata
        description: "{{ doc('metadata') }}"

version: 2

models:
  - name: silver__nft_la_liga_editions
    description: |-
      Cleaned EditionCreated events.
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - event_contract
            - edition_id
